"""피처 계층 — 시세를 학습 가능한 표로 바꾼다.

    입력   일별 시세 (날짜 · 종목 · 시가 · 고가 · 저가 · 종가 · 거래량)
    출력   피처 표 X (행=시점, 열=지표) + 레이블 y (등락 방향)

⚠️ 이 계층이 프로젝트에서 가장 사고가 잦은 곳이다
----------------------------------------------
모델보다, 백테스트보다, 여기서 틀린다. 이유는 **틀려도 에러가 안 나기 때문**이다.

세 가지를 특히 조심한다.

1. **미래 정보 누출(look-ahead)**
   `df["ma20"] = df["close"].rolling(20).mean()` 은 안전하지만
   `df["close"].rolling(20).mean().shift(-1)` 은 미래를 본다.
   지표를 만들 때 t 시점 행은 **t 시점까지의 자료만** 써야 한다.

2. **전체 구간 정규화**
   `StandardScaler().fit(X_all)` 은 검증 구간의 평균·분산을 학습에 흘린다.
   스케일링은 **폴드 안에서** 학습 구간만 보고 맞춘 뒤 검증에 적용한다.

3. **레이블 정렬**
   "내일 오르는가" 레이블은 t 행에 t+1 의 답을 붙인 것이다. 마지막 행은 답이 없어
   버려야 하는데, 그냥 두면 NaN 이 0 으로 채워져 조용히 틀린다.

두 개의 층이 있다 (2026-09-04 기준)
----------------------------------
  지표 계산(numpy)      indicators.py · volume.py · volatility.py · returns.py ·
                        dynamic_threshold.py
                        — 순수 함수, `timeseries/transform.py`와 스타일을 맞춤(pandas 안 씀).
                        서로 import 하지 않는다(같은 층의 독립성).

  데이터셋 조립(pandas)  model_dataset.py · stock_model_dataset.py · market_breadth.py
                        — 후보 선정·시점 정합·표 조립. pandas가 자연스러운 작업이라
                        numpy 원칙의 예외로 둔다. 서로 import 할 수 있다(같은 층의 조합).

새 파일을 추가할 때는 어느 층인지 먼저 정하고, 그 층의 스타일을 따른다.
"""
