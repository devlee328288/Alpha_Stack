# 2026-09-15 · 누른 RUN 이 받은 것은 먼저 계산된 결과였다

| | |
|---|---|
| 작성 | 이동원 |
| PR | [#303](https://github.com/devlee328288/Alpha_Stack/pull/303) 배포 화면 다시 캡처 · 문서 PR(이 TIL 이 실린 PR) — 결과 보고서 v1.2 · 발표 v1.1 · 평가파트 v1.4 |
| 반영한 PR | 강민석 님 [#287](https://github.com/devlee328288/Alpha_Stack/pull/287) · [#289](https://github.com/devlee328288/Alpha_Stack/pull/289) · [#300](https://github.com/devlee328288/Alpha_Stack/pull/300) · [#301](https://github.com/devlee328288/Alpha_Stack/pull/301) · [#302](https://github.com/devlee328288/Alpha_Stack/pull/302) (전부 본문 · 댓글 없음) |
| 브랜치 | `docs/deploy-capture-20260915` · `docs/final-report-v1.2-20260915`(앞 브랜치 위에 쌓음) |

## 1. 무엇을 했나

마감일 아침, main 에 들어온 마지막 자료(#300 · #301 · #302)를 문서에 옮겼다.

- 09:31 — #299 가 main 에 들어간 것을 `is-ancestor` 로 확인하고 GitLab 을 `827de1d → cff6715` 로 맞췄다(#287 · #289 · #300 · #301 · #302 가 함께 올라감).
- 09:35 — Space 가 이미 수정 뒤 코드로 빌드됐는지 **RUN 없이 화면 글자로** 확인했다.
- 09:44~09:47 — 10개 페이지를 다시 찍어 새 폴더에 올렸다(#303).
- 결과 보고서 v1.2(5.7절 · 6장 ⑥⑧ · 9.1절 · 부록 A · B), 발표 최종본을 `docs/발표/version1.1/` 로 옮김, 평가파트 v1.4(대시보드 문서를 `cff6715` 로 다시 잼).

## 2. 왜 했나

보고서 5.7절과 부록 A 는 *"Space 를 다시 빌드한 뒤 교체한다"* 는 약속을 적어 두고 있었고, 배포 화면 폴더는 조합 E · BEST MODEL LightGBM 시점이었다.
강사님께 내는 날 그 약속이 빈 채로 남으면 보고서가 스스로 미완성이라고 말하는 셈이었다.

## 3. 어떻게 했나 — 고른 것과 버린 것 (사용자 결정 2026-09-15)

| 갈림길 | 고른 것 | 버린 것 |
|---|---|---|
| 캡처 폴더 | 새 폴더 `2026-09-15` · 09-14 는 조합 E 기록으로 남김 | 09-14 폴더에 덮어쓰기 — 폴더 날짜와 찍은 날이 어긋난다 |
| 루트의 `발표_최종통합본.html` | `docs/발표/version1.1/` 로 `git mv`(내용 무수정) | 링크만 걸고 루트에 둠 · 반영하지 않음 |
| 평가파트 | 새 판 v1.4 | v1.3 제자리 보강 · 이번 세션에서 안 함 |
| 발표 자료와 코드 · 원본의 어긋남 | **이 TIL 에만** 적는다 (아래 5절) | 적지 않음 · 강민석 님께 이슈 |
| Universe RUN | 누르지 않음 (09-14 결정 그대로) | — |

## 4. 어떻게 확인했나

| 확인한 것 | 결과 |
|---|---|
| Space 가 수정 뒤 빌드인가 | 공개 API 는 `RUNNING` 만 준다. 화면에 `COMBINATION C` · `RUN ALL (A~F)` · `EXPERIMENT CONFIG · SSOT` 가 있다 → #301 뒤 빌드 |
| 대시보드 코드가 그 뒤 또 바뀌었나 | `git diff --stat 9cf3f2c cff6715 -- dashboard backtest` 빈 출력 |
| RUN 이 끝났나 | 대기 루프(2초 간격 세 번 조용함)가 **9초**로 끝나 의심 → 그림을 열어 표가 다 찼는지 봄 → Overview 결과 시각 UTC 09-14 23:21:49 · 23:22:25 = KST 08:21 · 08:22 가 누른 시각(09:44)보다 앞섬 → **서버 공유 캐시의 결과** |
| 목록표 숫자 | 캡처 때 함께 저장한 본문 글(`innerText`)에서 옮김 |
| 보고서 본문 번호 | 빌더 `doc_id_leaks` 통과 · Word **PDF 를 쪽마다** 넘김 → 부록 A 표가 두 줄 늘어 **빈 쪽 하나**가 생긴 것과 부록 B 경로가 v1.3 인 것을 찾아 고침 |
| 음수 비중이 공매도인가 | `backtest_5day_service._run_a5` 에서 보유 0 → `shares += qty`(음수) → 5일 뒤 되사기(`:129-133`) = 공매도. 어느 페이지도 import 하지 않음 |

## 5. 막힌 것 · 남은 것

**발표 최종본에서 본 어긋남** — 발표는 끝났고 자료는 동결이라 고치지 않았다(사용자 결정 · 이 TIL 에만).

| 슬라이드 | 적힌 것 | 원본 · 보고서 |
|---|---|---|
| 04-3 KOSPI200 결과 | 홀드아웃 Accuracy **0.4330** | `reports/final_holdout/final-holdout-20260911_report.json` 의 `0.4330543933…` → 넷째 자리 반올림 **0.4331**(보고서 · README). 슬라이드는 버림 표기 |
| 04-1 방법론 | 폴드 설계 `gap 5 (양 트랙 동일)` | 보고서 3.4절 · 8.2절도 gap 5 (그래서 T+6 청산일이 평가 첫날과 하루 겹친다) |
| 10-1 결론 | 기준선 설명 `12폴드 · gap 6` | 같은 발표 안의 04-1 과 다르다 |
| 07 검증 엔진 | `gap < 6 → LeakageError` · 발전 방향 장은 `gap 5 잠금` | 한 발표 안에 5 와 6 이 섞여 있다 — 코드의 판정식은 이번에 열지 않았다 |

**대시보드에서 본 것** (강민석 님 파트 · 평가파트 v1.4 §5 에 관찰로만)

- 화면 LogisticRegression 정확도 0.3736 ≠ 공식 개발구간 0.4542(같은 조합 C · 12폴드 · 720행) — 원인 미판정
- Overview 칸은 여전히 `BEST MODEL` · `A/B/C` 글자
- 조정 라벨이 주석은 시가 · 코드는 종가(`step5_optimize_6params.py:182-183`)
- 비용 프리셋은 왕복 이름인데 매수 · 매도에 각각 곱한다 · 손익분기 D · E 1.996% 는 탐색 상한 2% 바로 아래
- STOCK 스코프 라벨 ±1.0%(공식 ±2.0%)

**남은 것** — 홀드아웃 비용 차감 성과 · 사전등록 주 검정(사용자: 계산 안 함) · 모델파트 v1.3 세 파일(사용자 안내 대기) · #276 · #280 열림.

## 6. 배운 것

- **빨리 끝난 RUN 은 성공의 증거가 아니다.** 9초는 대기 루프의 최솟값과 똑같았다. 그림을 열고, 결과에 찍힌 시각을 누른 시각과 대 보고서야 "먼저 계산된 결과를 받았다"고 쓸 수 있었다. 공유 캐시가 있는 화면에서 "내가 돌렸다"는 말은 따로 증명해야 한다.
- **빌드 시각을 안 주는 곳에서는 바뀐 글자가 증거다.** PR 이 화면에 새로 넣은 낱말 하나(`RUN ALL (A~F)`)가 API 보다 정확했다.
- **표에 두 줄을 더하면 쪽이 하나 빌 수 있다.** docx 텍스트 검사는 통과했고, 빈 48쪽은 PDF 를 넘겨야만 보였다.
- **옮길 파일은 안에서 무엇을 부르는지 먼저 본다.** 발표 파일은 옆의 `dashboard.mp4` 를 불렀고, 그 영상은 처음부터 저장소에 없었다 — 옮겨도 새로 깨지는 링크는 없었다.
