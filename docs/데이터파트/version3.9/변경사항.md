# 데이터 파트 v3.9 — 변경사항

> 2026-09-07 저녁 · 이동원 · 이전 판 [v3.8](../version3.8/변경사항.md)
> 실측 기준 커밋 `e4dd849` (PR #162 머지 직후) · 환경 anaconda 3.12.13 · FinanceDataReader 0.9.202

---

## 한 줄로

**같은 날 오후, 코드가 먼저 움직인 것을 문서가 따라잡은 판**입니다. v3.8(아침) 뒤로 코드 PR 이
여덟 개 머지됐습니다 — #152(달력 제 단계) · #153(모델 fit 원장 복원) · #156(이상치는 어긋남으로 ·
재개일 규칙) · #157(v13 지수 기본키) · #160(조합 B~F 전부 계산) · #161(오준영 님 조합 A~F) ·
PR #162(인라인 넷을 원자 함수로) · PR #154(TIL). 문서는 v3.8 에 멈춰 있었고, 그래서 이 판은 **새 설계보다
대조표**가 먼저였습니다 — 무엇이 어느 문서에 들어가야 하는지를 코드 변경 13건에서 거꾸로 셌습니다.

그리고 하나를 새로 냈습니다 — 🆕 [데이터 품질 원장 설계](데이터_품질_원장_설계.md)(로드맵 ⑯ · 설계만).
오준영 님의 #159 에는 **지수만 있으면 되고 지수는 받을 수 있다**고 실측으로 답했습니다.

## 0. 대조표 — 코드 변경 → 들어간 문서

| 코드 (v3.8 기준 `4637bca` 이후) | 들어간 문서 |
|---|---|
| `pipelines/refresh.py` 7단계 · `calendar` 제 단계 (PR #152) | [기능명세 **v1.7**](../../기능명세/version1.7/버튼_갱신_파이프라인.md) §0.1 · 명세서 §2.9 D-90~92 🔴→✅ · 🆕 D-93 · §6 빚 `--days 10` 닫음 |
| `supply/adj_quality.py` 칸 넷 · `corporate_actions` 재개일 규칙 · 51종 재생성 (PR #156) | [품질 규약](데이터_품질_규약.md) 🆕 §4.4 · 🆕 §6.7 · §8 재분류 · 명세서 🆕 D-18 · §4 사전 |
| `migrations.py` v13 · `krx_index` 가드 → 기본키 (PR #157) | [ERD **v2.4**](../../erd/version2.4/ERD.md) · 명세서 D-32 · §6 빚 닫음 |
| `features/` 원자 함수 4개(#147) · 인라인 넷 → 원자 호출(#162) · `models/stock_*` 조합 A~F(#161) · 폴드별 기준선(ADR 0006) | [핵심코드 **v1.2**](../../핵심코드/version1.2/) 7편 · 명세서 🆕 §2.11 (09-07 오후 확정값 전부) |
| `docs/README.md` 가 데이터파트 v3.7 · 피처파트 v1.0 을 가리킴 · `데이터파트/README.md` 에 v3.8 행 없음 | README 전수 대조 → v3.9 · v1.1 · v3.8·v3.9 행 추가 |
| ⚠️ 판을 닫기 직전에 머지된 PR #163(조합 비교 표본·중복 피처 정정) · **#164(조합 G — 방향축 × 크기축 상호작용)** | **이 판의 실측은 전부 `e4dd849` 기준**이라 §2.11 의 조합별 유효행은 #163 뒤에 달라질 수 있다. 조합 G 는 노트북 06/11 이 제안한 것을 오준영 님이 구현하신 것 — 다음 판(모델파트 v1.1)에서 적는다. 노트북 06/12 는 `1d0fe45` 로 다시 돌려 같은 값을 확인했다 |

## 1. ⑪ 버튼 갱신 파이프라인 — 설계에서 구현으로 (PR #152 · 기능명세 v1.7)

v3.8 명세서는 D-90~92 를 여전히 🔴 "설계" 로 적고 있었는데, 코드는 09-05 에 이미 구현됐고
09-07 에 일곱 번째 단계가 생겼습니다.

| | v3.8 문서 | **v3.9 (코드 그대로)** |
|---|---|---|
| 상태 | D-90~92 🔴 설계 | **D-90~93 ✅** |
| 단계 | (설계) 다섯 | **일곱** — `ingest` · 🆕 `calendar` · `adj`(🔴 기본 끔) · `gate` · `verify` · `export` · `upload` |
| 달력 | `adj` 안에서만 (꺼 두는 단계) | **제 단계 · 매번 · 19초 · 끄는 스위치 없음** — 09-02~04 사흘치 달력이 비어 `known_at` 계산이 설 뻔했다 |
| 시험 | 25건 | **28건**(함수 26 · 달력 3건 추가) |
| 09-07 실행 | — | 아침 재배포 `20260907-101640`(20분) · `--only calendar` `113105`(19초) · 오후 첫 시도 `132312` 는 **검증기가 막았다**(종목기본정보 (날짜,시장) 6쌍 미수집) → 받고 `134654` 로 재배포 |

"붉으면 안 올라간다" 가 실제로 작동한 첫 기록이 남았습니다.

## 2. 🆕 D-18 이상치 — 크기가 아니라 어긋남으로 (PR #156 · #132 · 품질 규약 §4.4 · §6.7)

규약 §4.2 는 "공급 층은 표시를 붙여 내보낸다" 고 적어 두고 표시가 없었습니다. 오준영 님의
`|수익률| > 100%` 필터가 지운 **1건**을 재현하니 KRX 등락률과 일치하는 **진짜 사건**(경남에너지
+153.66%)이었고, 반대로 신세계 −60.6%(KRX +14.95%) 같은 오류는 크기 필터에 걸리지 않았습니다.

| 무엇 | 실측 |
|---|---|
| 판별 함수 | `supply.adj_quality.attach_adjustment_quality` — `adj_return_1d` · `adj_change_rate_gap` · `is_adj_suspect`(gap > **1%p**) · `is_extreme_return`(\|r\| > 30% · 의심 아님) · 시험 16건 |
| 허용폭 근거 | 0.15~1.0%p 띠 12,271행의 94% 가 종가 5,000원 미만 — 1원 눈금 반올림 |
| 원인 | **재개일 규칙**이 인적분할·회생 재상장에 "주식수 배율 = 가격 배율" 을 적용 — chain 52건(51종목) 을 DART 공시로 전수 대조 (인적분할 24 · 회생·합병+감자 28) |
| 표준 대조 | 인적분할은 CRSP·Xignite·Daloopa 셋 다 시장가 기준 배당형 조정 · 회생 재상장은 CRSP 가 시리즈를 끊는다 · KRX 는 둘 다 기준가를 새로 정한다 → **기준가 / 전일종가** |
| 재생성 | **51종목만** (`--codes`) · 값 바뀐 행 39,166 · FDR 창 밀림 49행 · `verify()` ① 52쌍 → **0쌍** |
| 전 시장 | 의심 1,406 → **1,353**(chain 119 · fdr 1,234) · 극단 중 의심 149 → 103 · **후보군 의심 0** |
| 🔴 영향 범위 | 처음 38종목 → 코드로 세니 **51종목** (㉢ 가지를 빠뜨렸다) |

## 3. v13 — 지수 기본키에 시장 (PR #157 · #144 · ERD v2.4)

v3.8 은 `krx_index._시장가드` 로 **받기 전에 막았고**, v3.9 는 답을 가드가 아니라 **기본키**로
옮겼습니다 — `PRIMARY KEY (bas_dd, index_name, index_class)`. 새 표를 만들고 옮기고 이름을 바꾸는
방식이고, `index_class` 가 빈 행이 있으면 세웁니다(막다른 길이 아니게 무엇을 하라고 적는다).
표 21개는 그대로, `tests/test_migrations.py` 가 새로 생겼습니다.

## 4. 개별종목 학습 표본 — 처음으로 전수 실측 (명세서 🆕 §2.11 · 노트북 06/11)

오준영 님 #155 제안(조합 B~F)에 **전부 계산해서** 답했고(PR #160), 그 과정에서 표본 자체를 처음
쟀습니다. 값은 명세서 §2.11 에 표로 있습니다. 여기서는 배운 것만 —

- **영향 범위는 구현한 코드가 도는 자리에서 센다.** 정지일이 전 시장 2.94% 인데 학습표본에서는
  **0.016%** 다. 전 시장 비율로 말했으면 180배 과장이었다.
- **효과크기 하나로 줄을 세우면 무엇을 맞히는지 놓친다.** 단변량 1위 변동성이 방향을 못 맞힌다
  (상승·하락이 나란히 늘 뿐). 방향축과 크기축을 갈라야 한다.
- **값이 같아도 가는 길이 다르면 언젠가 갈린다.** 인라인 넷 vs 원자 함수 3,315,889행 대조에서 셋은
  최대차 0.0, `hv_regime` 만 73행 — `_rolling_mean` 누적합 정밀도. PR #162 로 호출을 통일했고 73행은
  신장환 님께 셋 중 하나를 여쭤 뒀다.
- 중립대 ±2.0% 는 **ADR 이 없다** — 그러나 지수 ±1.0% × 변동성비 2.195 = ±2.20% 라 일관된다(#158).
  폴드별 적용 기준선 31.71~47.64% · 평균 38.54%([ADR 0006](../../decisions/0006-개별종목-중립대와-평가기준선.md)).

## 5. #159 — 앞당기려면 종목이 아니라 지수가 필요하다 (노트북 06/12)

| | 지금 | 지수 2008~ 연장 |
|---|---|---|
| 조합 E 첫 사용 가능일 | 20110127 (준비구간 **269**거래일) | **20090204** |
| 공통 첫 날 | 20110127 | 20100201 (종목 F 가 병목) |
| 첫 검증일 | 20140217 | **20130212** (**249거래일** 앞당김) |
| 마지막 검증일 | 20240822 | 그대로 |

FDR 지수 캐시(GitHub CSV · `FinanceData/fdr_krx_data_cache` · 2000 년부터)는 2008~2009 **501행**을
주고, 겹치는 닷새의 종가·전일비·등락률·거래량이 우리 KRX 값과 **전부 같습니다.** 종목은 FDR(3,000거래일
상한)·KRX Open API(2010-01-04 부터) 둘 다 0행 — 그러나 필요한 것이 종목이 아닙니다. 🔴 캐시 저장소에
LICENSE 가 없어 넣는다면 출처·리비전을 대장에 적습니다. 넣는 방식 두 갈래(a: `index_price` 에 `source`
칸 · b: 별도 `index_price_warmup.parquet`)를 댓글로 여쭸고, 권고는 **(b)**. 오준영 님이 "현재 결정은
유지" 라 하셔서 **지금 넣지는 않습니다**(로드맵 ⑰).

## 6. 🆕 ⑯ 데이터 품질 원장 — 설계 ([문서](데이터_품질_원장_설계.md))

게이트가 이미 재는 것(`check_data.py` · 검증기 4종 · `PROFILE.json`)과 아직 아무 데도 안 남는 것
(`is_adj_suspect` 수 · 축별 중복 · 달력 정합)을 **표 하나**로 모아 반출본과 함께 HF 카드에 게시하자는
설계입니다. 원장은 막지 않고 적습니다 — 막는 것은 지금처럼 게이트가 합니다. 구현은 다음 판.

## 7. 숫자 갱신 (2026-09-07 저녁)

| | v3.8 | **v3.9** |
|---|---|---|
| 시험 | 1,197 (KOSDAQ 7건 포함) | **1,312 passed · 3 xfailed · 4분 06초** (anaconda 3.12.13) |
| ruff | 3건 | 3건 (전부 강민석 님 파일 — 변화 없음) |
| DB | v12 · 6,487 MB | **v13 · 6,561 MB** · 시세 9,231,938 · 달력 12,315 · 종목기본정보 9,229,173 |
| 반출본 | 7,888,945 × 23 | 7,888,945 × **24** · 지수 171,435 × 12 |
| HF | 22파일 | 22파일 (09-07 두 번 재배포) |
| 노트북 06/ | 11 | **12** (#159 · 그림 2장) |

## 8. 남은 것 · 빚

명세서 §6 에 전부 있습니다. 새로 올린 것 — `hv_regime` 73행(`_rolling_mean` · 신장환 님 답 대기) ·
#159 준비구간 파일(오준영 님 결정 대기) · FDR 구간 의심 1,234행 · 품질 플래그가 반출본에 없음(⑯) ·
`adj_close` 사흘 8,294행(의도적 보류) · 오준영 님 스크립트의 HF 경로 고정(관찰만).
닫힌 것 — `--days 10` · TIL 표 · `index_price` 기본키.

---

## (이하 v3.8 내용)

# 데이터 파트 v3.8 — 변경사항

> 2026-09-07 · 이동원 · 이전 판 [v3.7](../version3.7/변경사항.md)
> 실측 기준 커밋 `4637bca` (PR #145 머지 직후)

## 한 줄로

**선택으로 열어 두었던 KOSDAQ 업종을 실제로 받은 판**입니다. 팀장이 18장을 직접 내려받아
36장 40,874행이 되었고(격리 0 · 전량 대조 일치), 그러자 **KOSPI 18장으로는 보이지 않던 것**이
드러났습니다 — KRX 화면이 한 종목을 두 업종에 싣습니다(17쌍). `supply.sector` 가 결정적
규칙으로 하나를 고르고 `industry_ambiguous` 로 그 사실을 남깁니다.

🔴 그리고 **제가 자료를 잃고 되찾은 판**이기도 합니다. KOSDAQ 업종지수를 받으려다
`index_price` 의 기본키에 시장이 없다는 것을 놓쳐 KOSPI 업종지수 17종 40,324행을
덮어썼습니다. 완전 복구했고, 같은 일이 없도록 **받기 전에 막는 문**을 세웠습니다(→ #144).

---

## 1. ✅ 업종 KOSDAQ 18장 — 선택에서 **완료**로 (PR #145)

| 무엇 | 실측 |
|---|---|
| 스냅샷 | **36장**(KOSPI 18 + KOSDAQ 18) · **40,874행** — v3.7 은 18장 16,650행 |
| 판별 | 18장 전부 KOSDAQ 으로 갈림 · 기준일 정확 일치 · 종가 **30/30종** |
| 반입 | 들임 18 · **격리 0** · 거부 0 (행 24,224) |
| 전량 대조 | 종가·시가총액·시장 다른 행 **0** |
| `industry` 채움 | 개발구간 기준 **41.6% → 거의 100%** (KOSDAQ 종목이 7,888,945행 중 4,599,390행 = 58.3%) |

받기 전 시험 7건으로 예측해 둔 것이 그대로 맞았습니다. 가이드 §8 을 "받을지 정한다" 에서
**"받았다"** 로 고치고, 받고 나서 안 것 셋을 §8.5~8.8 로 더했습니다.

## 2. 🔴 한 종목이 업종 둘에 실린다 — 결정적으로 하나로

KOSPI 18장에는 **0건**이라 그동안 보이지 않았습니다. KOSDAQ 에서 **34행(17쌍) · 4종목**
(글로웍스·해성산업·솔본·신라섬유)이 `부동산` 과 `일반서비스` 에 동시에 들어 있습니다.

그냥 두면 `industry_as_of` 가 종목당 두 행을 내주고, `attach_industry` 의 `merge_asof` 는
둘 중 **아무거나** 집습니다 — 어느 업종이 붙을지가 입력 순서에 달려 **재현되지 않습니다.**
행 수는 안 늘어 눈에 띄지도 않습니다.

**고르는 규칙**: ① 같은 스냅샷에서 종목 수가 많은 업종 → ② 동수면 업종명 사전순.
①은 그 스냅샷 안만 보므로 `as_of` 규약을 지키면서, **KRX 자신의 사후 정리와 17/17 일치**
합니다. 사전순만 쓰면 **0/17 로 반대**가 됩니다.

**DART `sic_nm` 은 못 씁니다** — 커버리지 5.6%, 해성산업 값은 `known_at` 2025-10-08 이라
2010년 판정에 쓰면 미래참조입니다.

### 🆕 반출 칸 하나 — `industry_ambiguous`

고른 결과만 주고 고른 사실을 감추면 되짚을 수 없습니다. 품질 플래그를 배포본까지 흘리는
것이 실무 관행이라 반출 칸에 넣습니다. 참인 행은 개발구간에서 **4종목**뿐이고, 업종이 안
붙은 행은 `NaN`(*"모호하지 않다"* 가 아니라 *"모른다"*)입니다.

## 3. 🔴 index_price 기본키 결함 — 자료를 잃고 되찾았다 (→ #144)

`PRIMARY KEY (bas_dd, index_name)` 에 **시장이 없습니다.** KOSPI 와 KOSDAQ 은 `건설`·
`금속`·`화학` 같은 **같은 이름의 업종지수를 각각** 가져서, KOSDAQ 을 받자 `INSERT OR REPLACE`
가 KOSPI 행을 덮어썼습니다.

| | 값 |
|---|---|
| 덮어쓰인 것 | KOSPI 업종지수 **17종 × 40,324행** (20170102~20260904) |
| 🔴 행 수 | **늘었습니다** — 196,272 → 244,108. 축(`index_class`)별로 세고서야 드러났습니다 |
| 복구 | KOSPI **51종 196,272행** · 오염 흔적 0일 · KOSDAQ 잔여 0행 (오늘 아침과 동일) |
| HF 배포본 | **영향 없음** (09-05 자) |

복구 중 하나 더 배웠습니다 — 건너뛰기 판정은 `collect_log` 가 아니라 **`index_fetch_log`**
(옛 대장)를 봅니다. 그것만 지우면 재수집이 전부 건너뜁니다.

지금은 `krx_index._시장가드` 가 **받기 전에** 막습니다. 경고가 아니라 차단인 이유는 이
사고가 겉으로 성공처럼 보이기 때문입니다. 푸는 방법(기본키에 `index_class` 를 넣는
마이그레이션 **v13**)까지 문구에 적어 막다른 길로 두지 않았습니다.

## 4. ✅ HF 재배포 — 팀원이 받는 자료가 크게 좋아졌다

갱신 규칙 **②개발구간 값 변경**과 **③반출 규격 변경**이 동시에 걸려 재배포했습니다
(`python -m pipelines.refresh` · run_id `20260907-101640` · 전 단계 ok · 11분).

| 무엇 | 09-05 배포본 | 2026-09-07 |
|---|--:|--:|
| `daily_price_dev.parquet` | 7,888,945행 × **23칸** | 7,888,945행 × **24칸** |
| `industry` 채움 | **41.6%** | **98.1%** (7,739,853행) |
| ├ KOSPI | 99.0% | 99.0% |
| └ KOSDAQ | **0%** | **97.4%** (4,455,559행) |
| 🆕 `industry_ambiguous` | 없음 | True **4,065** · False 7,735,788 · 결측 149,092 |

데이터셋 카드도 함께 새로 만들어져 새 칸이 설명과 분포까지 들어갔습니다
(`industry_ambiguous` · bool · 결측 1.89%). 반출 게이트의 검사기 4종은 전부 통과했습니다.

> ⚠️ 시세 자체는 **20260904(금)가 최신이고 그게 맞습니다.** 09-05는 토요일입니다.
> 세션 중 "금요일 시세가 빠졌다" 고 잘못 본 것을 여기 적어 둡니다 — 날짜만 보고
> 요일을 확인하지 않았습니다.

## 5. 검증기가 시장을 안다

| 절 | v3.7 | v3.8 |
|---|---|---|
| §1 중복 | 겹치면 무조건 🔴 | 겹침은 원자료 성격(ℹ️) · **골라낸 뒤에도 남았나**를 본다 |
| §3 업종 흐름 | 두 시장 합산 (`부동산` 이 안 사라진 것처럼 보였다) | **시장별** · 2024 개편 검사는 KOSPI 기준 |
| §4 조인 | KOSPI 지수에 KOSDAQ 이름을 맞춰 18장 전부 🔴 | **시장별** · 지수 0종인 시장은 문제로 세지 않는다 |
| 종료코드 | 1 | **0** |

---

## (이하 v3.7 내용)



## 한 줄로

**팀원 작업이 들어온 만큼 데이터 파트가 움직인 판**입니다. 강민석 님이 백테스트 원장에 되먹임 칸
아홉을 뚫으셨고(PR #131), 그 원장이 **학습에 들어갈 수 있는 모양인지 재는 계약**을 데이터 파트가
냈습니다(⑮ 1차 · PR #135). 신장환 님의 극단수익률 실측(#132)에 맞춰 **84 → 83(개발 76 · 홀드아웃 7)**
로 구간을 갈라 적었고, 선택 항목이던 **업종 KOSDAQ** 은 받기 전에 코드가 어떻게 다루는지 시험
7건으로 실측해 가이드 §8 로 열어 두었습니다. 받는 것은 여전히 선택입니다.

---

## 1. ⑮ 백테스트 되먹임 — 원장 계약 검증기 (PR #135 · #110)

| 무엇 | 어디 | 실측 |
|---|---|---|
| 원장 칸 | `supply/backtest_ledger.py` `SIGNAL_LOG_COLUMNS` **17** · `TRADE_LOG_COLUMNS` **22** | `run_backtest()` 를 실제로 돌려 셌다 — 정규식으로 센 "23" 이 틀렸다 |
| 검증기 | `verify_execution_log` · `verify_trade_log` — 칸 9 · 확률 합 1 · 체결일 > 예측일 · 홀드아웃 행 · 실현수익률 재계산 · **t+5 종가가 봉인 안이면 비어 있어야** · 라벨 · 식별 | DataFrame 만 받는다. `backtest/` 를 import 하지 않는다 |
| 시험 | `tests/test_backtest_ledger_contract.py` **33건** (규격 21 + 종단 12) | anaconda · `.venv` 양쪽 통과 |
| 🔴 돌려 보고 안 것 | HF 원장 2종 **미업로드** · `.venv` 에서 `backtest_strategies.py` **import 불가**(`datasets` 최상단) · `hash(Timestamp)` 시드라 **실행마다 예측이 다름**(1,985 → 1,898행) | #110 에 질문으로 |

상태표 D-95(가칭) 로 등재하지 않고 **로드맵 ⑮** 를 "1차 완료 · 조인 정문은 원장이 올라온 뒤" 로 바꿨습니다.
반출 게이트 등록(`VERIFIERS`)과 `daily_price`·`adj_close`·라벨 조인은 원장이 HF 에 올라온 뒤입니다.

## 2. 업종 KOSDAQ — 받기 전에 코드를 실측했다 (가이드 §8 · 시험 7건)

| 층 | 결과 |
|---|---|
| 판별 `infer_sector_date` | KOSDAQ 종가로 되짚어도 기준일·시장이 맞는다. 같은 날 KOSPI·KOSDAQ 파일은 시장으로 갈린다 |
| 변환본 `sector_text` | 시장구분 칸이 없어도 `KOSDAQ` 이 앞에 붙는다 · 이름 `업종분류현황_KOSDAQ_YYYYMMDD` |
| 반입 `sector.json` | `market` enum 에 이미 `KOSDAQ` |
| 공급 `industry_as_of` | 같은 날짜면 함께 · `market=` 으로 가른다. 🔴 **날짜가 어긋나면 최근 스냅샷 하나만** — 그래서 같은 18개 날짜로 받는다 |
| 반출 `attach_industry` | 종목별 `merge_asof` 라 어긋나도 둘 다 붙는다 |

기대 행 수는 `daily_price` 로 쟀습니다 — KOSDAQ 2010 1,036 · 2020 1,408 · 2024 1,704 · 2026 1,830.
**받지 않았습니다.** 필요로 하는 곳이 없습니다(2안은 KOSPI 만으로 되고 KOSPI200 트랙은 업종을 안 씀).
KOSDAQ 업종지수 대조표는 받는 사람이 생기면 3시점 실측으로 만듭니다.

## 3. 극단 수익률 — 84 가 아니라 83 (개발 76 · 홀드아웃 7)

신장환 님이 #132 에서 개발 구간 76행을 실측하셨고, 문서의 84 와 8 이 달랐습니다. 지금 DB 로
`adj_close` 하루 변화 ±100% 초과를 `LAG` 로 다시 세니 **전 구간 83 = 개발 76 + 홀드아웃 7**
(20100120 ~ 20260623). 84 는 09-04 값이고 09-05 전 종목 재빌드에서 한 행이 줄었습니다.
문서에 구간을 안 적었던 것이 원인이라 이제 셋을 함께 적습니다.

## 4. 평가·피처 파트 문서 (PR #134)

- 평가파트 **v1.1** — ARIMA 동반(PR #130) · 원장 칸 9개(PR #131) · 워크포워드 두 체계 대조 · PR 문구와 코드가
  다른 여섯 곳을 질문으로 · 평가파트·피처파트 **README** 신설
- 기준선 표 하나에 근거가 셋 → [#133](https://github.com/devlee328288/Alpha_Stack/issues/133) 회의 안건 4가지

## 5. 숫자 갱신 (2026-09-06)

| | v3.6 | v3.7 |
|---|---|---|
| 시험 | 1,132 passed (데이터 파트 세션) → 1,157 (09-05 저녁) | **1,190 passed · 3 xfailed · 3분 09초** (anaconda 3.x · +33 원장 계약 · +7 KOSDAQ 은 이 판 뒤 1,197) |
| ruff | 0 | 저장소 **3건** — 전부 강민석 님 파일(E501 · `fit_best` 이중 import) · 공백 23건은 PR #134 에서 정리 |
| 시세 | 9,223,644 | **9,231,938** (20260902~04 · 버튼 갱신 파이프라인) |
| HF 데이터셋 | 6 | 6 (원장 2종 미업로드) |

🔴 **기준선 숫자에는 환경을 붙입니다.** 이전 판의 1,157 은 anaconda 파이썬 값이고, README 가 정본이라
적은 `.venv` 는 `FinanceDataReader` 가 빠져 있어 1,136 + 수집 오류 1 입니다(빚에 올림).

## 6. 남은 것 · 빚

- 🔴 `.venv` 가 pyproject 와 어긋난다 — `FinanceDataReader` 없음 · `datasets` 없음(이건 의도). `uv pip install -e ".[dev]"` 로 맞춘다
- 🔴 anaconda 에 `pandas-2.2.2.dist-info`(파일 하나 `DELVEWHEEL`) 잔재 — `datasets` import 를 깨뜨린다. 지우려면 사용자 확인
- `docs/TIL/README.md` 이동원 표가 09-02 에서 멈춤 (09-03~09-06 열다섯 장 미등재)
- ⑮ 2차 — 원장 HF 업로드 뒤 조인 정문 · 게이트 등록 · 되먹임 피처(`known_at` = 체결일 다음 거래일)
