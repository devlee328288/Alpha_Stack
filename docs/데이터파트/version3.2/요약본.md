# 데이터 파트 v3.2 — 한 장 요약

> 2026-09-03 · 이동원 · 전문은 [명세서](명세서.md) · 설계는 [고도화 설계](고도화_설계.md) · [강사님 저장소 도입](강사님저장소_분석과_도입설계.md)

## 무엇이 서 있나 (전부 실측 · 2026-09-03)

| 자료 | 규모 | 시점 정합 |
|---|---|---|
| 종목 시세 `daily_price` | 9,223,644행 · 3,677종 · 4,102거래일 · 2010-01-04~2026-09-01 | T+1 0시 (계산) · ✅ `adj_*` 4칸 100% (v9) |
| 지수 시세 `index_price` | 196,119행 | 〃 |
| 재무 `dart_financial` | 662,933행 · 350종 · FY2015~2025 | **접수일 100%** (기록) |
| 거시 `macro_series` | 17,851행 · 9종 | `known_at` (계산) |
| 텍스트 | **0행** (`dart_disclosure` 표만) | → v11 `text_signal` 설계 (D-80~84) |

DB 는 SQLite 하나(`krx_cache.db` · v9 · 1,975 MB · 표 17개 — [ERD v2.1](../../erd/version2.1/ERD.md)).
테스트 848 passed + 3 xfailed · ruff 0건(데이터 파트 · `evaluation/` 6건은 강민석 님 몫).
HF `qurious-quant/alphastack-krx-dev` 7,888,945행 × 20칸 + `PROFILE.json` (9/2 재배포).

## 이번 판 — 고도화 **설계** (코드 0줄)

| 갈래 | 결론 |
|---|---|
| HF 인코더 | 후보 21종 실측 → 세 제약 통과 **6종**. 원료는 **DART 공시 제목**. 시작은 제로샷 `xlm-roberta-large-xnli`(MIT · 한국어 문장 긍정 0.988). 처음엔 토큰에 추론 권한이 없어 전부 403 → 팀장이 켠 뒤 **6종 실제 동작 확인** (콜드 스타트 최대 24.9초) |
| 버튼 갱신 | `pipelines/refresh.py` — 잠금(`filelock`) → 증분(달력으로 창 계산) → 게이트 → **판정이 "재배포 필요" 일 때만** HF |
| 에이전트 관측 | `(W, F)` float32 규격 + 마스크 채널. `supply` 정문 재사용. 2차 구현 |
| GitHub Stars | 18곳 실측 → `filelock` · `act` · Gymnasium 계약 채택. 팩터 트랙은 2차(개수 실측부터) |
| 강사님 저장소 | FastAPI 골격 도입(⑩) · 화면은 Streamlit(강민석) · **Actions 는 봇 커밋 빼고 검사만** |

## 팀원은 이렇게 씁니다

- **학습 자료**: HF `alphastack-krx-dev` — 수익률·라벨은 `adj_close`, 시가총액만 `close`
- **재무**: HF `alphastack-dart` 의 **pit/** 만 (bulk/ 는 접수일이 없어 학습 금지)
- **키**: `python scripts/check_keys.py --live` — 10종 자가 점검 · [가이드 v3.2](API키_발급_가이드.md)
- **코드로 조회**: `supply.index_series(as_of=...)` — `as_of` 없이는 안 열립니다

## 다음

HF 토큰 권한 → 6종 재실측 → ⑨ v10 공공데이터포털 → ⑪ `refresh.py` → ⑫ v11 `text_signal` → CI(팀 합의 후).

## 조심할 것 세 가지

1. 첨부·검색 결과의 라이선스를 믿지 않는다 — **모델 카드**만 (8/18건이 달랐다)
2. "403" 은 셋 중 하나다 — 모델 없음 · 서버 미제공 · **내 토큰 권한 없음**. "400" 도 둘이다 — 미제공 · **경로 다름**
3. 버튼이 HF 까지 올리는 것은 **판정이 2 일 때만** — 새 시세만으로는 안 올린다
