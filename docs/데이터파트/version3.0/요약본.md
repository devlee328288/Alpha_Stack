# 데이터 파트 v3.0 — 한 장 요약

> 2026-09-02 · 이동원 · 전문은 [명세서](명세서.md)

## 무엇이 서 있나 (전부 실측 · 2026-09-02)

| 자료 | 규모 | 시점 정합 |
|---|---|---|
| 종목 시세 `daily_price` | 9,223,644행 · 2010-01-04~ | T+1 0시 (계산) · 🔴 수정주가 아님 → v9 |
| 지수 시세 `index_price` | 196,119행 | 〃 |
| 재무 `dart_financial` | 662,933행 · 350종 · FY2015~2025 | **접수일 100%** (기록) |
| 거시 `macro_series` | 17,851행 · 9종 | `known_at` (계산) |

DB 는 SQLite 하나(`krx_cache.db` · v8 · 1,650 MB · 표 16개 — [ERD v2.0](../../erd/version2.0/ERD.md)).
테스트 813 passed + 3 xfailed · ruff 0건.

## 팀원은 이렇게 씁니다

- **학습 자료**: HF `qurious-quant/alphastack-krx-dev` 에서 다운로드 (개발구간만 · parquet 권장)
- **재무**: HF `alphastack-dart` 의 **pit/** 만 학습에 사용 (bulk/ 는 접수일이 없어 금지)
- **코드로 조회**: `supply.index_series(as_of=...)` — `as_of` 없이는 안 열립니다
- **파일 보내기**: `data/inbox/` → 반입 엔진이 판정 + 보고서
- 막히면: `python scripts/check_hf_access.py` (HF) · `reports/inbox/` (반입 판정)

## 줄 단위 해설 (신설)

[핵심코드 v1.0](../../핵심코드/version1.0/) 7편 — 수집 · 반입 · 공급 · 반출·HF ·
피처 · 모델 · 평가. 코드를 안 열어 본 사람 기준으로 썼습니다.

## 다음

⑧ **v9 수정주가 4칸 + 거래일 달력** (#51 · PR #71 머지 시 HF 재배포 동반)
→ ⑨ 공공데이터포털 5종 → ⑩ FastAPI + 스케줄러.

## 조심할 것 세 가지

1. `close` 로 수익률 계산 금지 — 분할 미조정 (v9 의 `adj_*` 를 기다리세요)
2. 재무 직접 집계 시 기본키에 `account_detail` 포함 — 빼면 6.4% 소실
3. CSV 왕복은 값을 바꿉니다(1 ULP·빈문자열→NaN) — parquet 을 쓰세요
