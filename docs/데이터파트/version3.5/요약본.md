# 데이터 파트 v3.5 — 한 장 요약

> 2026-09-04 · 이동원 · 전문은 [명세서](명세서.md) · 이번 판의 이유는 [변경사항](변경사항.md)

## 무엇이 서 있나 (전부 실측 · 2026-09-04)

| 자료 | 규모 | 시점 정합 |
|---|---|---|
| 종목 시세 `daily_price` | 9,223,644행 · 3,677종 · 4,102거래일 · 2010-01-04~2026-09-01 | T+1 0시 (계산) · `adj_*` 4칸 100% · 🆕 **감자 보정이 정본 코드로** (`+ca_fix` 43,778행) |
| 지수 시세 `index_price` | 196,119행 | 〃 |
| 재무 `dart_financial` | 662,933행 · 350종 · FY2015~2025 | **접수일 100%** (기록) |
| 거시 `macro_series` | 17,851행 · 9종 | `known_at` (계산) |
| 🆕 공시 목록 `dart_disclosure` | **1,555,556행** · 법인 2,651 · 2010~2026-09-04 | 접수일 (기록) |
| 🆕 텍스트 신호 `text_signal` | 고유 제목 **18,600** → 반출 **1,295,176행** | 접수일 **다음 거래일** (계산) |
| 종목기본정보 `stock_base_info` (v11) | 9,220,879행 · 우선주 판별 정본 | 기준일 다음 거래일 (계산) |
| 신원·법인 `stock_identity` · `corp_profile` (v10) | 4,197,242행 · 40,569행 | 계산 · **관측** |

DB 는 SQLite 하나(`krx_cache.db` · **v12** · 6,487 MB · 표 **21개** — [ERD v2.3](../../erd/version2.3/ERD.md)).
테스트 **1,120 passed** + 3 xfailed · ruff 0건(데이터 파트 · `backtest/` 7건은 강민석 님 몫).
HF `qurious-quant/alphastack-krx-dev` **22파일** — `full/`·`small/` + 종류별 `identity/ financial/ macro/ calendar/ text/`.

## 이번 판 — 두 세션을 한 번에

| 갈래 | 결론 |
|---|---|
| 🆕 감자 보정 **정본화** | `scale_series` ⑤ — 관문 둘(원가격 검산 · FDR 미반영)로 편다. 스크래치 보정이 **감자 두 번인 종목에서 하루를 빠뜨린** 결함까지 찾아 닫았다 · 시험 10건 · 16종 재빌드 · 못 편 자리 0/17 |
| 🆕 극단 수익률 | **32행 → 0행.** ±100% 초과 84행이 전부 정리매매·정지재개·자본변동으로 설명된다 |
| 🆕 텍스트 신호 (v12) | 만들었다 — 그리고 **5일 방향과 관계가 없다** (Cramer's V 0.026 · 설정 12조합 전부 < 0.04). 로컬 CPU · 0원 · 7분. `report_nm` 을 피처로 쓰면 **시점이 샌다** (같은 문장 15개 연도가 11~81%) |
| 🆕 포털 경로 틀 둘 | 같은 `_V2` 가 세대가 다르다 → 호스트만 남기고 `EP_*` 17개가 경로를 갖는다 · 예탁결제원 `B552481` 은 XML·선언 파라미터만·상한 200 — **별도 클라이언트** · 천장 `99991231` |
| 감자 (v3.4 판) | FDR 이 감자를 조정 안 하던 17자리를 09-04 오전 스크래치로 보정 — 그 코드가 저장소에 없어 v3.4 가 빚 1순위로 올렸고, 이 판이 닫았다 |
| 🆕 반출 게이트 | 검증기 4종이 `upload_to_hf` 를 막는다 — 붉으면 안 올라간다 · 대장 3종(`MANIFEST`·`_reference`·`_text`) |
| 규칙 개정 | **인코더는 로컬 추론이 기본** (v3.2 "로컬 다운로드 Won't" 뒤집힘) · 생성형·파인튜닝은 여전히 Won't |

## 팀원은 이렇게 씁니다

- **학습 자료**: HF `alphastack-krx-dev` — 수익률·라벨은 `adj_close`, 시가총액만 `close`
- **재무**: `financial/` 또는 `alphastack-dart` 의 **pit/** 만 (bulk/ 는 접수일이 없어 학습 금지)
- **신원**: `identity/` — `crno` 로 종목 ↔ 법인 ↔ 재무를 잇는다 · 외국기업 `0000000000000` 은 먼저 거른다
- **공시 감성**: `text/` — 넣어도 안 오른다는 실측이 카드에 있다 · `report_nm` 은 피처가 아니다
- **키**: `python scripts/check_keys.py --live` · [가이드](API키_발급_가이드.md)
- **코드로 조회**: `supply.index_series(as_of=...)` — `as_of` 없이는 안 열립니다

## 다음

⑬-b 전 종목 재빌드 + HF 재배포(판정부터) → ⑭ 중기·장기 피처 → ⑮ 백테스트 되먹임([이슈 #110](https://github.com/devlee328288/Alpha_Stack/issues/110) 답 대기) → ⑩ FastAPI · ⑪ refresh · CI(합의 후).

## 조심할 것 세 가지

1. **p 값이 아니라 효과크기를 본다** — 126만 행에서 p < 1e-300 은 아무것도 말해 주지 않는다
2. **스크래치로 고친 것은 고쳐진 것이 아니다** — DB 가 맞아도 만든 방법이 없으면 다시 깔 때 돌아온다
3. **계수를 믿기 전에 원가격으로 검산한다** — 인적분할·출자전환은 주식수가 크게 움직여도 가격이 연속이다
4. **텍스트 피처는 "언제 적혔나"가 새는지 본다** — 제목 속 연월이 시장 국면을 가리킨다
