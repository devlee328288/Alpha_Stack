# 프로젝트 계획서 — 버전 목록

> 최신 버전 폴더가 정본입니다. 옛 버전은 **발전 과정의 기록**이라 지우지 않습니다.
> 규약 전문은 [docs/README.md](../README.md).

| 버전 | 날짜 | 무엇이 담겼나 |
|---|---|---|
| [**version4.0**](version4.0/) ⭐ | 2026-09-05 | **일정표를 빼고 그 자리에 「용어와 산출물 사전」을 넣었다(본표 숫자 교체 → Major)** — 팀원이 "ARIMA 동반·폴드 커버리지·시도 횟수 계측이 무엇을 하라는 말인지 모르겠다"고 물었고, 다시 보니 이름만 있고 뜻이 없었다. 용어 14개 × [말·무엇인가·왜 필요한가·**무엇을 만들면 끝인가**] · 기준선 재실측(52.64%→**38.50%** · 54.99%→**41.11%**) · 폴드 커버리지 실측 **20.3%** · 부록 C 2주 일정과 [그림 1] 간트 삭제 · 부록 E 실측표 2행→5행 · 부록 G 미결 ④ 갱신 · 그림 번호 재배열 |
| [**version3.0**](version3.0/) ⭐ | 2026-09-03 | **프로젝트명 Qurious · 발표자 신장환·강민석 공동 확정(본표 → Major)** + 데이터 파트 고도화를 일정·기능에 반영 + **HTML 산출** — 부록 A 선택범위 ⑪ 실측으로 고쳐 쓰고 ⑭ 갱신 파이프라인 · ⑮ 에이전트 관측 규격 · ⑯ 검사용 CI 추가 · 부록 C 이동원 열 9/2·9/3 실적 + 9/4~ 고도화 순서 · 부록 D 착수 자산 2026-09-03 실측 · 부록 E 홀드아웃 2년 주석 · 부록 G 미결 6 → 4 · 부록 H "데이터 계층 계약" 열 · **[`프로젝트계획서.html`](version3.0/프로젝트계획서.html)** 이 docx 와 같은 목록에서 구워진다 |
| [version2.1](version2.1/) | 2026-08-29 | **부록 I 의 그림이 사실과 반대로 읽히고 있었다** — [그림 2] 를 계층아키텍처(supply 문 하나)에서 시스템아키텍처(**문 둘**)로 교체 · 문이 둘인 이유 문단 추가 · `DIAGRAM_DIR` 을 아키텍처 v1.2 로 |
| [version2.0](version2.0/) | 2026-08-29 | **팀이 꾸려지고 숫자가 실측으로 바뀌었다** — 팀명 `적층` → `Qurious` · 팀원 4명 확정 · 주제 선정 이유를 실측 수치로 · 목표 5 → 8(팀원 제안 3건) · 부록 A~F → A~I |
| [version1.0](version1.0/) | 2026-08-24 | **강사님께 제출한 최초 계획서.** 팀원 모집 중(3명) · 팀명 `적층` · 프로젝트명 `AlphaStack` |

---

## 정본은 docx 가 아니라 **코드**입니다

⚠️ 이 폴더의 `.docx` 는 **산출물**입니다. 손으로 고치지 마세요.

내용의 정본은 [`scripts/build_project_plan.py`](../../scripts/build_project_plan.py)
안에 있고, docx 는 거기서 구워 나옵니다.

```bash
python scripts/build_project_plan.py
```

**왜 이렇게 하나.** 계획서는 한 번 쓰고 끝나는 문서가 아닙니다. 킥오프에서 미결이
닫히고 실측이 갱신될 때마다 고쳐야 하는데, Word 에서 손으로 고치면 **무엇이 왜
바뀌었는지 git 이 못 봅니다.** 내용을 코드에 두면 `git diff` 로 읽힙니다.

실제로 v2.0 은 네 번 고쳐졌고, 그 네 번이 전부 커밋으로 남아 있습니다 — 계획서
초판(`94874c2`) · 착수자산 수치 정정(`6123025`) · 종목/지수 결론 정정(`4d64ca5`) ·
유의임계 중첩보정(`e50c75d`).

### 스타일은 v1.0 에서 물려받습니다

`python-docx` 기본 템플릿은 한글 폰트(eastAsia)가 잡혀 있지 않아 Word 에서 글꼴이
어긋납니다. 그래서 스크립트가 **v1.0 docx 를 열어 본문만 비우고 다시 채웁니다.**
`version1.0/프로젝트계획서.docx` 를 지우면 빌드가 멈춥니다.

---

## 다음 버전을 팔 때

```bash
# ① scripts/build_project_plan.py 의 내용을 고치고
# ② 같은 파일의 OUT_DOCX 를 새 버전으로 올린다 (예: version2.2)
python scripts/build_project_plan.py
# ③ version2.2/변경사항.md 에 무엇이 왜 바뀌었는지 적는다
python scripts/check_doc_links.py    # 상대 경로가 바뀌므로 반드시 돌린다
```

⚠️ **옛 버전의 docx 를 다시 굽지 마세요.** `.docx` 는 zip 이라 내용이 같아도
타임스탬프 때문에 바이트가 달라지고, 그대로 커밋하면 이력에 수백 KB 짜리 blob 이
쌓입니다. 산출물은 **새 버전 폴더에만** 만듭니다.

### 무엇이 바뀌었는지 확인하는 법

docx 는 `git diff` 가 안 읽히므로 본문을 뽑아 대조합니다.

```python
from docx import Document
doc = Document("docs/계획서/version2.1/프로젝트계획서.docx")
for p in doc.paragraphs:
    if p.text.strip():
        print(p.text)
for t in doc.tables:
    for row in t.rows:
        print(" | ".join(c.text.strip() for c in row.cells))
```

### Major(+1.0) 인가 Minor(+0.1) 인가

| | 언제 | 예 |
|---|---|---|
| **Major** | 본표(9칸)의 값이 바뀐다 | 팀명·프로젝트명 확정 · 목표/분석방법 항목 증감 · 팀원 변동 |
| **Minor** | 부록·수치만 바뀐다 | 실측값 갱신 · 부록 문구 다듬기 · 일정 조정 |

킥오프(9/1)에서 프로젝트명·발표자·합격선이 정해지면 **본표가 바뀌므로 v3.0** 입니다.
