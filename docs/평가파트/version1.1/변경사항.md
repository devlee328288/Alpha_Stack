# 평가파트 v1.1 변경사항 (2026-09-06)

> v1.0(09-05) 이후 머지된 강민석 님 PR 둘([#130](https://github.com/devlee328288/Alpha_Stack/pull/130) ·
> [#131](https://github.com/devlee328288/Alpha_Stack/pull/131))을 옮겨 적고, 코드로 잰 것을 붙였습니다.
> 실측 기준 커밋 `0bf504e`.

## 1. 파일 이름이 바뀌었습니다

| v1.0 | v1.1 |
|---|---|
| `기준선_최적화와_피처_계약.md` | `동적기준선_ARIMA동반_백테스트원장.md` |

다루는 것이 둘(피처 계약 · 동적 기준선)에서 넷(+ ARIMA 동반 · 백테스트 원장)으로 늘어
제목을 내용에 맞췄습니다. v1.0 파일은 그대로 남습니다.

## 2. 새로 들어온 것

| 절 | 무엇 | 근거 |
|---|---|---|
| §3 | **ARIMA 동반** — step5 가 폴드마다 `fit_best` 로 새로 적합해 143폴드 평균 39.85% | PR #130 |
| §4 | **폴드 커버리지 20.3%** 확정 · **시도 횟수 계측은 아직 없음**(`trials.jsonl` 0바이트 · `log_trial` 저장소에 없음) | PR #130 · 실측 |
| §5 | **백테스트 원장 칸 9개** — `signal_log` 8→17 · `trade_log` 14→23 · `predict_5d_after` 가 확률까지 반환 · HF 업로드 코드 | PR #131 |
| §2.1 | **워크포워드 두 체계 대조** — step5 는 sliding 143폴드 · 504/63/21 · gap 0, 합의 체계는 expanding 12폴드 · gap 5 | 실측 · [#133](https://github.com/devlee328288/Alpha_Stack/issues/133) |

## 3. 🔴 실측이 PR 문구와 다른 곳 — 질문으로 남겼습니다

| PR 문구 | 코드 | 어디 |
|---|---|---|
| CMA-ES 상한 21,450 = 143 × 150 | `max_evals=300` 기본 · 정밀 모드도 300 → 143 × 300 = **42,900** | `step5:224` · `run_full_pipeline.py:81` |
| `log_trial` 함수와 JSONL 파일 생성 | JSONL 은 0바이트로 있고, **함수는 저장소에 없음** | 전수 grep |
| 원장 HF 업로드 구현 | 코드는 있고 **실행은 안 됨** — `qurious-quant` 데이터셋 6개 그대로 | `HfApi().list_datasets` |
| 검증 구간 60일 | `val_days = 3 × 21 = 63` | `step5:224` |
| ARIMA 를 5일 라벨과 비교 | 예측은 **1일** 수익률 재귀 · 비교 대상은 5일 `label` | `step5:332,352,371` |
| `threshold_pipeline` 전체 정확도 41.08% | `y_true` 가 **1일** `pct_change` +0.5%/−0.3% | `run_full_pipeline.py:69-70` |

어느 것도 "틀렸다" 가 아니라 **회의에서 하나로 맞출 것**입니다. #133 §5 에 결정 4가지로 올렸습니다.

## 4. 남은 것 표 갱신 (§7)

- ✅ 지움: "백테스트 원장을 HF 에 올릴지 (#110 답 대기)" → 답이 왔고 칸이 들어갔다. 대신 **"업로드 실행"** 이 남았다
- 🔁 바뀜: `ruff` 57건(#120 · 09-05 에 0건으로 닫힘) → PR #130·#131 로 **26건** → 이 PR 에서 기계적 23건 정리 → **3건**(E501 · F401 · F811) 강민석 님 몫
- 🆕 더함: #133 회의 결정 4가지 · `trials.jsonl` 계측 · `n_trials=50` 치환

## 5. 이 PR 이 평가 파트 코드에 한 것

`ruff check --fix --select W293,I001,F541,W292` 로 **공백·import 정렬·f-string 접두사·EOF 개행만**
고쳤습니다(3파일 · 23건). 코드 줄은 바꾸지 않았고, `step5` 의 `sys.path.insert` 는 여전히
`timeseries` import 앞에 있습니다(`:9` → `:15`). 남은 3건은 코드 줄을 고쳐야 해서 손대지 않았습니다.
