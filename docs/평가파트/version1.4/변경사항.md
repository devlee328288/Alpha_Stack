# 평가파트 변경사항 — v1.3 → v1.4

> 2026-09-15 · 문서화 이동원 · 코드는 강민석 님 (PR #287 · #289 · #300 · #301) · 실측 main `cff6715` · HF Space 화면 09:44~09:47 KST

**Minor 개정입니다.** 새 문서를 더하지 않고 [대시보드 9페이지와 배포](대시보드_9페이지와_배포.md)를 v1.3 이 *"다음 판에서 다시 잰다"* 고 남긴
자리(§1 페이지 줄 · §5)부터 main `cff6715` 기준으로 다시 쟀습니다. 새 판을 판 것은 #301 이 Backtest · Cost Sensitivity 의 **구조**(설정 공유 · 전략 6개)를
바꿨기 때문입니다(사용자 결정 2026-09-15).

## 무엇이 바뀌었나

| | 절 | 무엇 |
|---|---|---|
| ✏️ | §0 | 한 줄 아래에 09-14 오후 네 PR 의 요지 — 조합은 공식 모델과 같아졌고, 설정은 하나를 공유하고, 최고 모델은 여전히 조화평균 |
| ✏️ | §1 | 표에 **"v1.3 뒤 바뀐 것"** 열 · Overview(`BEST MODEL` · `A/B/C` 글자 그대로) · Baseline(#300 판정 함수 · 🔴 주석은 시가 · 코드는 종가) · Model Lab(#287 · #289 조합 C · K · #300 칸 이름) · Backtest(#301 전략 A~F · 5거래일 락) · Cost Sens(#301 Backtest 설정을 받는다) · Universe(조합 K). Comparison · Risk · System · QFRS 는 코드 변경 0 을 `git log` 로 확인 |
| 🆕 | §2 | mermaid 에 `combo_config` 와 Backtest → Cost Sens 설정 흐름 · 캐시 표에 `backtest_service` 네 캐시 · 🆕 **§2.1 설정 공유** — `ExperimentConfig` 칸과 `cache_key()` · `signal_key()` · #267 ML 의견 원문과 지금 코드의 차이(`session_state` 에서 읽는다) |
| 🆕 | §3.3 | 수정 뒤 빌드 확인 — 공개 API 는 빌드 시각을 주지 않아 **화면 글자**로 확인 · 캡처 10장 |
| ✏️ | §4 | #264 · #265 · #267 · #268 **닫힘**(09-15 09:00 · 닫는 댓글 없음) · 열에 **"지금 코드"** — STOCK 라벨 ±1.0% 그대로 · 비용은 매수 · 매도 각각 그대로 · 설정 공유는 구현 · #280 은 열림(조합 ✅ · 정렬 🔴) · #276 행 |
| ✏️ | §5 | ruff 209 → **220**(`dashboard/` 199 → 206 · 그 밖 10 → 14, 파일 구성도 바뀜) · 화면 LR 정확도 0.3736 vs 공식 0.4542 · 손익분기 탐색 상한 2% · 비용이 가장 클 때 D · E Sharpe 가 다시 오름 · `backtest_5day_service.py` 는 아무도 import 하지 않고 공매도 비중을 쓴다 |
| ✏️ | §6 | 최고 모델 칸을 공식 규칙으로 · 비용 단위를 이름에 · 공식 홀드아웃 예측을 Backtest 입력으로 |

## 그대로인 것

[동적 기준선 · ARIMA 동반 · 백테스트 원장](동적기준선_ARIMA동반_백테스트원장.md) — v1.2 · v1.3 과 같은 파일입니다.
#300 이 `step5_optimize_6params.py` 의 `make_band_labels` 를 바꿨지만, 이 문서는 그 함수의 판정식을 적지 않았으므로 고칠 문장이 없습니다. 바뀐 판정식은 대시보드 문서 §1 Baseline 줄에 적었습니다.

## 재현

```bash
git log --oneline 5e70ee1..cff6715 -- dashboard/ backtest/ evaluation/ features/model_dataset.py models/
git diff --stat 9cf3f2c cff6715 -- dashboard backtest          # 비어 있어야 한다
python -m ruff check . --statistics
python scripts/check_doc_links.py
```
