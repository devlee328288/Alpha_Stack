# 조사 — 근거 보관소

> **여기 있는 것**: 명세서가 *"그래서 무엇을 만드나"* 를 짧게 말할 수 있도록, **왜 그렇게
> 정했는지의 원문**을 옮겨 둔 곳입니다. 문헌·실호출·계측 결과가 들어옵니다.

## 왜 폴더를 따로 팠나

1차 명세서를 2주 분량으로 좁히면서, 조사 기록이 명세서 안에 그대로 남아 있으면 **읽는
사람이 "무엇을 만드나"에 도달하기 전에 지칩니다.** 실제로 요구사항 v1.0 은 389줄 중
**160줄이 팀원 제안 하나에 대한 조사**였습니다.

그렇다고 지우면 안 됩니다. 이 프로젝트가 파는 것이 *"결론이 진짜냐"* 이고, **근거가
그 답**이기 때문입니다. 그래서 **명세서는 짧게, 근거는 여기 온전히** 둡니다.

---

## 버전이 없는 이유

조사는 **한 시점의 관측**입니다. 관측을 나중에 고치면 그건 관측이 아닙니다.
틀린 것이 나오면 **본문을 덮어쓰지 않고 "이관 뒤 확인된 정정" 블록을 덧붙입니다.**
[docs/README §2](../README.md) 의 버전 규약이 명세서용인 것과 같은 이유입니다.

> 📌 ADR([decisions/](../decisions/))과의 차이: ADR 은 **결정**이고 조사는 **관측**입니다.
> *"protego 를 쓴다"* 는 ADR 이고, *"urllib.robotparser 가 fail-open 이더라"* 는 조사입니다.

---

## 목록

| 문서 | 무엇을 답하나 | 쓰인 곳 |
|---|---|---|
| [예측 지평](예측지평.md) | 1거래일 이진 vs 5거래일 3분류. **1거래일은 맞혀도 비용에 진다**(손익분기 65.03% vs 문헌 상한 55%대). ADR-0002 의 미측정 2건을 닫았고, 기준선 정의 오류를 찾았다 | ADR-AS-0002 유지 근거 · 팀원 제안 회신 |
| [시장 균열 스코어](시장균열스코어.md) | 팀원 제안을 문헌 + 우리 데이터로 검정한 결과. **방향 기각 · 크기 채택**, 그리고 **라벨 정렬이 상관을 10배 부풀린다**는 발견 | F-30 (2차) · F-19 누수 시연의 재료 |
| [공시 시점 정합](공시시점정합.md) | DART 접수일을 어떻게 얻는가. `rcept_no[:8]` 편법을 **4,800건 실측으로 기각** — 3건이 어긋났고 **둘은 미래를 3일 먼저 본다** | 데이터파트 D-12 |
| [도구 선정 근거](도구선정.md) | `robots.txt` 파서 4종 비교(표준 라이브러리가 **fail-open**) · SQLite **900만 행 DDL 계측** · 화면 스택 버전 호환성 | D-03 · 마이그레이션 · 화면 |
| [반입 자료 — 합류와 대조](반입자료-합류와-대조.md) | 남의 자료를 우리 시세와 어떻게 잇나. **조사가 낸 답이 반대심문에서 3표 기각됐다** — 인용문 5건이 출처에 없었고, 막겠다는 구멍은 아직 없었으며, `loaded_at` 을 `as_of` 에 거는 것은 `clock.py` 가 이미 기각한 설계였다. 마이그레이션 다음 번호는 v8 이 아니라 **v6** | 대조 설계 · `supply/` 경계 유지 |
| [외부 API 실측](외부API-실측.md) | **우리 네이버 키는 개발자센터가 아니라 API HUB 것이다**(호스트·헤더·한도가 전부 다름) · `start+display-1<=1000` 부등식은 **없다**(1,099건 도달) · **KRX 10,000/일은 약관 명문**(제8조 ④, 키당) · 호스트 오인 시 **HTTP 200 에 오류가 실린다** | D-04 예산 · D-20 뉴스 수집기 |

---

## 쓸 때의 규칙

- **추정과 실측을 섞지 않습니다.** 재지 않은 값은 *"미측정"* 이라고 적습니다.
- **재현 방법을 함께 적습니다.** 스크립트가 있으면 명령줄까지.
- **유리한 쪽만 재지 않습니다.** 우리 결론에 불리한 관측도 그대로 싣습니다 —
  [시장균열스코어 §6.2](시장균열스코어.md) 가 그 예입니다(팀원 가설과 부호가 반대).
- **구간을 밝힙니다.** 개발구간인지 전구간인지 홀드아웃인지에 따라 값이 달라집니다.
  ⚠️ 봉인 홀드아웃 수치는 **기본으로 재지 않습니다.**
