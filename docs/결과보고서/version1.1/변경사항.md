# 결과 보고서 v1.1 — 내부 번호를 걷어내고, 로드맵을 확장 가능성으로 바꾼 판

> 2026-09-14 · 이동원 · **강사님 제출용** · 정본 [`scripts/build_final_report.py`](../../../scripts/build_final_report.py) ·
> 산출물 [`결과보고서.docx`](결과보고서.docx) · [`결과보고서.html`](결과보고서.html) · 그림은 바뀌지 않아 [v1.0 그림](../version1.0/그림/)을 그대로 가리킨다
> 숫자 기준 main `827de1d` · 5.7절 대시보드 코드 확인은 main `bcf2864` (16:52) · Word 로 목차를 채워 **50쪽** (v1.0 46쪽 · 17:00 KST) · 표 29 (v1.0 30 — 로드맵 표를 뺐다) · 그림 4
> **Minor** — 요약서 핵심 결과의 숫자는 하나도 바뀌지 않았다. 결론 칸의 마지막 문장만 다음 차수 전제를 뺐다

## 한 줄 요약

사용자 피드백 둘(**AI 스러운 문구** · **ADR 같은 내부 번호**)과 함께 받은 검토 의견 셋(비용 차감 성과 · 대시보드 불일치 · 다음 스텝)을
반영했습니다. 숫자는 그대로 두고 **문장 · 용어 · 9장 구성**을 바꿨습니다.

## 받은 피드백과 반영한 자리

| 피드백 (2026-09-14) | 반영 | 보고서 자리 |
|---|---|---|
| ① AI 스러운 문구체 제거 · 다듬기 | 평서문 "~했다" 로 전면 재작성 — 격언처럼 끝나는 문장 · 줄표로 반전을 거는 제목 · 가운뎃점 나열을 걷어냈다 | 전 장 |
| ② ADR 등 우리 문서에서 쓰는 것 대신 풀어서 · 쉽고 정확하고 자세하게 | 본문(부록 B 앞)의 결정 기록 · 이슈 · PR 번호 **0곳**. 결정 기록은 이름과 날짜로 부르고, 번호 · 경로는 부록 B 한 표로 모았다. 내부 용어(공급 계층 · 사후값 · gap 등)는 처음 나올 때 풀고 부록 C 에 모았다 | 전 장 · 부록 B · C |
| 비용 차감 성과(수익률 · MDD · Sharpe)가 가장 큰 빈자리 | **계산하지 않음** — 사용자 결정. 빈자리를 "왜 비었고, 연결하면 무엇을 확인하나" 로 풀었다 | 4.5절 · 9.2절 ① |
| 대시보드 Model Lab 과 공식 모델(C · K) 불일치 | 보고서에 **현재 상태**를 코드로 확인해 적었다 — PR 직전 확인에서 #287 · #289 머지를 찾아 5.7절을 다시 썼다(아래 6절) | 5.7절 · 6.1절 ⑧ · 9.1절 |
| "다음 스텝" 스토리텔링 (딥러닝 · 공시 본문 · 대안데이터 가설 1~2줄) | 9장의 "2 · 3차 로드맵" 표를 빼고 **확장 가능성 여섯**을 가설 · 주의할 점 · 확인 방법으로 | 9.2절 |
| 다음 프로젝트로 안 넘어갈 수도 있으니 확장 가능성으로 (사용자) | 다음 차수를 전제한 문장을 모두 뺐다 | 1.1절 · 요약서 결론 · 산출물 ⑥ · 6.2절 ⑮ |

## 정한 것 — 사용자 선택 (2026-09-14)

| 갈림길 | 고른 것 | 버린 것 |
|---|---|---|
| 홀드아웃 비용 차감 성과 | 계산 안 함 · 확장 가능성으로 서술 | 공식 예측 파일로 직접 계산(v2.0 Major · 강민석 · 오준영 님 확인 필요) · 강민석 님께 이슈로 요청 |
| 대시보드 불일치 | 보고서에 현 상태만 · #287 머지와 Space 재빌드 뒤 캡처 교체(별도 PR) | 강민석 님께 첫 화면 안내 문구 요청 · 내가 안내 문구 PR(남의 파트 · #287 과 충돌) |
| 번호를 어디까지 | 본문은 전부 풀고 부록 B 에만 | 부록까지 제거 · ADR 만 풀고 이슈 · PR 은 둠 |
| 문체 | 서술형 "~했다" 다듬기 | 연구보고서 개조식 "~함" · 경어체 "~합니다" |
| PR 직전에 #287 · #289 머지를 찾은 뒤의 순서 | 지금 5.7절을 고쳐 PR · 캡처 교체는 Space 재빌드 뒤 별도 PR(보고서 반영은 v1.2) | Space 재빌드 · 캡처 뒤 한 번에 · 문장만 최소 수정 |
| 머지 뒤에도 BEST MODEL 이 조화평균 | 보고서에만 사실로 적음 | #280 에 확인 댓글 1건 · 적지 않음 |
| 캡처할 때 Universe RUN | 누르지 않음 | 기준선만 RUN · 전부 RUN |

## 무엇을 바꿨나

### 1. 결정 기록을 번호 대신 이름으로 부른다

| 본문에서 부르는 이름 | 파일 (부록 B) |
|---|---|
| 예측 대상 결정 (2026-08-26) | `docs/decisions/0002-예측대상과-레이블.md` |
| 사전등록 문서 (2026-08-26) | `docs/decisions/0004-사전등록.md` |
| 홀드아웃 기간 결정 (2026-09-02) | `docs/decisions/0005-홀드아웃-2년.md` |
| 중립 구간과 기준선 결정 (2026-09-07) | `docs/decisions/0006-개별종목-중립대와-평가기준선.md` |
| 모델 선정 규칙 결정 (2026-09-08) | `docs/decisions/0007-최종-모델-선정과-홀드아웃.md` |
| 예측 대상 확장 제안 (2026-09-09 · 미채택) | `docs/decisions/0008-예측대상-확장-제안.md` |
| 홀드아웃 실행 설정 결정 (2026-09-11 · 승인 오준영) | `docs/decisions/0009-최종-홀드아웃-실행설정.md` |
| 마지막 구간 평가 결정 (2026-09-11 · 오준영) | `docs/decisions/0010-개봉후-마지막-예측구간.md` |
| 선정 기준 논의 · 대시보드 불일치 논의 · 문서 정정 제안 | 이슈 #216 · 이슈 #280 + PR #287 · PR #278 |

- 사람 이름은 **문서 안에 적힌 경우에만** 붙였다(0009 승인 · 0010 결정 = 오준영). 0002 ~ 0007 은 최초 커밋 작성자가 모두
  `devlee328288` 이라, 커밋 작성자를 결정한 사람으로 읽지 않았다.
- 원문에 번호가 박힌 인용 한 곳(마지막 구간 평가 결정)은 `[사전등록 문서와 홀드아웃 실행 설정 결정]` 처럼 대괄호로 바꾼 자리를
  표시하고 출처에 그 사실을 적었다.

### 2. 문장 — 이런 것을 고쳤다

| v1.0 | v1.1 |
|---|---|
| 1.2 **문제 정의 — 이길 수 있는 폭이 좁다** | 1.2 **문제 정의** |
| 5.1 **두 트랙 모두 기준선을 넘었다 — 그런데 기준선이 낮아졌다** | 5.1 **기준선 대비 결과와 중립 비율의 변화** |
| "이 목록이 비어 보이는 프로젝트는 대개 재 보지 않은 것이다." | "알고 있지만 아직 해결하지 못한 한계를 정리했다." |
| "“0행” 은 틀린 방법이 같은 자리에서 실제로 샌다는 것을 보인 뒤에야 뜻을 갖는다." | "잘못된 방법에서 누수가 실제로 발생하는 것을 먼저 확인했으므로, 0행이라는 결과를 누수 차단의 근거로 볼 수 있다." |
| "어느 것도 에러를 내지 않았고, 결과 숫자를 그럴듯하게 만들었을 것이다." | "모두 오류 메시지 없이 지나갔고, 발견하지 못했다면 결과 숫자를 실제보다 좋게 만들었을 수 있다." |
| 10장 소제목 "① 기본값이 곧 정책이다" | "① 검증 함수의 기본값이 누수를 허용하고 있었다" |
| "무슨 일이 있었나 — …" · "무엇을 바꿨나 — …" | "상황: …" · "조치: …" |
| "변동성 수준 자체가 라벨의 절반을 설명하는데(스피어만 1.0000)" | "변동성 수준이 라벨과 강하게 연결된다. 20일 변동성 십분위와 비중립 비율의 스피어만 상관은 1.0000이었다." — 순위상관 1.0 은 "절반을 설명" 이 아니므로 정확한 말로 바꿨다 |

그 밖에 v1.0 은 7장 첫 절에 제목이 없어 목차가 7.2 부터 시작했다 → **7.1 날짜별 변경 내역**을 붙였다.

### 3. 9장 — 로드맵에서 확장 가능성으로

| 확장 | 가설의 출발점 (이 보고서의 결과) | 주의할 점 · 확인 방법 |
|---|---|---|
| ① 홀드아웃 비용 차감 백테스트 연결 | KOSPI200 상승 Precision 이 상승 비율의 **0.99배** · 현금 153일이지만 실제 하락 138일 중 **104일**에 매수 신호 → Sharpe 우위 · MDD 감소 모두 크지 않을 가능성 | 공식 예측 파일에 사전등록 절차(왕복 0.05% · 블록 10일 · 10,000회 · 시드 20260901)를 그대로 |
| ② 순서 정보를 쓰는 딥러닝(LSTM · GRU) | 지금 모델은 하루 시점 값 6~14개를 독립 행으로 본다 | **트리 모델 3종도 로지스틱 회귀를 넘지 못했다** — 복잡도만으로 오르지 않는다 · 같은 12폴드 · 다중검정 함께 |
| ③ 공시 본문 텍스트 | 제목 감성은 V **0.026** · 고유 제목 18,600개가 평균 84번 반복 | 제목의 결산 연월이 국면을 가리키는 누수(연도별 상승 비율 11.1~81.0%) · 효과크기로 먼저 판정 |
| ④ 재무 · 거시와 더 긴 지평 | 이미 시점 규칙으로 수집했으나 피처로 쓰지 않았다 | 지평이 바뀌면 손익분기 · 기준선부터 다시 계산하고 새로 사전등록 |
| ⑤ 하락 예측을 쓰는 {−1, 0, +1} | 개별종목 하락 재현율 0.4498 · **하락 예측 정밀도 0.3685 = 하락 비율 0.3487 의 1.06배** | 개인 공매도 비중 1.2% · 차입 비용 · 선정 기준 재결정 |
| ⑥ KOSDAQ 유니버스 | 변동성이 커 ±2.0% 중립 구간이 맞지 않을 것 | 3.1절 방법으로 중립 구간 재측정 · 스팩 · 관리종목 칸은 이미 있다 |

9.2절 머리에 **"새 확장을 확증적으로 평가하려면 새 봉인 구간이 필요하다"** 를 적었다 — 이번 홀드아웃은 이미 열었으므로 그 구간으로
새 모델을 고르면 개발구간과 같다.

### 4. 계획서 표는 고치지 않고, 옮겨 싣는 방식을 바꿨다

계획서 v6.0 은 이미 냈으므로 `build_project_plan.py` 는 건드리지 않았다. 보고서 빌더의 `_plain_rows()` 가 가져온 표를 이렇게 옮긴다.

| 바꾸는 것 | 예 |
|---|---|
| 결정 기록 번호 `(ADR 0007)` 제거 · "결정 기록" 행 삭제 | 라벨 · 설계 · 결과 · 한계 표에서 7곳 |
| 줄표 ` — ` → `: ` · 가운뎃점 나열 ` · ` → `, ` | "구현 — 5일 방향과…" → "구현: 5일 방향과…" |
| 내부 용어 7쌍 | 공급 계층 → 기준일 조회 모듈 · 틀린 대조군 → 잘못 결합한 비교군 · 설정 지문 → 설정 해시 · 5슬리브 → 5개 슬리브 · 관문 → 최소 조건 · 여덟 → 8개 |
| 9.1절 한계 표의 오른쪽 열 이름 | "발전 방향" → "대응 방향" (9.2절 확장 가능성과 구분) |

⚠️ 1차 빌드 PDF 를 넘기다 **본문은 "기준일 조회 모듈" 인데 가져온 QFRS 표는 여전히 "공급 계층"** 인 것을 봤다. docx 텍스트 검사는
번호만 봤기 때문에 두 벌 용어는 PDF 에서만 보였다 → 용어 7쌍을 더하고 다시 구웠다.

### 5. 빌더가 본문을 검사한다

- `DOC_ID_PATTERN` (`ADR` · `이슈 #` · `PR #` · `#숫자`)으로 부록 B 앞의 모든 글(문단 · 표 칸 · 캡션 · 인용 출처)을 훑고, 하나라도 있으면
  **굽지 않고** 위치와 할 일을 출력한다.
- **음성 대조군** — 번호 4곳을 일부러 심은 블록(문단 · 인용 출처 · 표 칸 · 캡션)과 부록 B 뒤 번호 1곳으로 돌려 **4곳을 모두 잡고 부록은
  건너뛰는 것**을 확인했다. 실제 본문 누출 0.

### 6. PR 직전에 뒤집힌 5.7절

16:12 에 "K · C 수정은 아직 main 에 합쳐지지 않았다" 로 썼다. PR 을 만들기 직전(16:52) fetch 에서 **#287 이 16:22, #289 가 16:33 에
머지**된 것을 찾았다(둘 다 작성자가 머지 · 본문과 댓글 없음). 머지만 보고 "고쳐졌다" 로 쓰지 않고, 합쳐진 코드(`bcf2864`)를 열어 갈랐다.

| 확인한 것 | 코드 근거 | 보고서에 적은 것 |
|---|---|---|
| 화면의 피처 조합 | `dashboard/services/combo_config.py` — MARKET `C`(6) · UNIVERSE · STOCK `K`(14) | 조합은 공식 모델과 같아졌다 |
| 최고 모델 선정 | `dashboard/services/comparison_service.py:41` — `sort_values("core_harmonic_mean")` | **여전히 조화평균** — 공식 선정 규칙과 달라 최고 모델이 로지스틱 회귀가 아닐 수 있다 |
| 공식 결과 실행기 | `models/final_holdout.py` · `scripts/run_final_window.py` 는 diff 에 없다. `features/model_dataset.py` 에 조합 `K` 와 `relative_ret_5_market` 0 대체가 더해졌지만 조합 `C` 목록과 라벨 계산은 그대로(줄바꿈만) · 종목 트랙은 `features/stock_model_dataset.py` | 4장 결과는 바뀌지 않는다 — 코드를 읽어 확인했고 테스트는 다시 돌리지 않았다 |
| HF Space | 공개 API `RUNNING` · 빌드 로그 API 는 인증을 요구 | 수정 뒤 재빌드 여부를 **확인하지 못했다** · 09-14 캡처(14:09~14:13)는 조합 E 시점 |

⚠️ 대시보드 개별종목 화면이 `relative_ret_5_market` 0 대체를 실제로 타는지는 판정하지 않았다(강민석 · 오준영 파트) — TIL 09-14 §12 에 적고 넘겼다.

## 숫자

v1.0 의 숫자를 그대로 쓴다 — 원본과 재측정 시각은 [v1.0 변경사항](../version1.0/변경사항.md)의 표. 이 판에서 **새로 계산하거나 새로 옮긴 값**은 아래뿐이다.

| 값 | 원본 |
|---|---|
| 개별종목 하락 예측 정밀도 3,696 / 10,030 = **0.3685** · 평가구간 하락 비율 8,217 / 23,567 = **0.3487** · 1.06배 | `reports/final_holdout/final-holdout-20260911_report.json` 혼동행렬 (2026-09-14 · 16:40 첫 빌드 전에 다시 계산) |
| KOSPI200 현금 보유 **153일** (하락 예측 58 + 중립 예측 95) · 실제 하락 중 상승 예측 104 / 138 = 75.4% | 같은 혼동행렬 |
| 공시 목록 1,555,556행 · 고유 제목 18,600 · Cramer's V 0.026 · 사업보고서 연도별 5일 상승 비율 11.1~81.0% | [데이터파트 v4.6 고도화 설계 §2.9](../../데이터파트/version4.6/고도화_설계.md) |
| 재무 662,933행 · 거시 17,864행 | 계획서 빌더 `DATA_ROWS` (v1.0 과 같은 값) |

## 비어 있는 자리 — 다음 판에서

| 무엇 | 채우는 조건 | 판 |
|---|---|---|
| 홀드아웃 비용 차감 수익률 · Sharpe · MDD · 사전등록 주 검정 | 공식 예측 파일이 백테스트 입력 계약으로 들어간다 | **Major** (요약서 핵심 결과 · 결론이 바뀐다) |
| 5.7절 마지막 문단 · 부록 A 캡처 비고 | #287 · #289 는 머지됨 → Space Factory rebuild(사용자) → 캡처 교체 PR | Minor (v1.2) |
| 5.7절 BEST MODEL 문장 · 6.1절 ⑧ | 대시보드가 최고 모델을 공식 선정 규칙으로 고른다 | Minor |
| 그림 제목의 줄표 (그림 2 · 4) | 그림을 새로 굽는 판에서 함께 | Minor |
| 7장 2026-09-14 행의 전언 | 모델 파트가 기준선 정의를 직접 글로 남긴다 | Minor |

## 재현

```bash
python scripts/build_final_report.py --word --pdf <스크래치>/report.pdf   # 본문에 번호가 남으면 여기서 멈춘다
python -m ruff check scripts/build_final_report.py
python scripts/check_doc_links.py
```
