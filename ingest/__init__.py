"""수집 계층 — 바깥 세상에서 자료를 가져와 우리 저장소에 넣는다.

    clients/   외부 API 와 HTTP 로 통신하고 응답을 정규화한다
    store/     정규화된 것을 저장하고 꺼낸다

이 둘을 나눈 이유
---------------
바깥은 우리가 통제하지 못한다. 응답 스키마가 바뀌고, 서버가 죽고, 필드가 잘려서
온다. 그 불확실성을 `clients` 안에 가둬 두면 `store` 위쪽은 "정규화된 시세"라는
안정된 모양만 보면 된다.

⚠️ 잘림은 조용히 온다
-------------------
KRX·DART 응답은 **에러 없이 일부만** 오는 경우가 있다. 원본 프로젝트가 이 방식으로
9종목을 잃은 적이 있다(원본 ADR-DS-0020). 그래서 수집한 건수가 예상과 크게 다르면
저장하지 않고 멈추는 것이 이 계층의 규칙이다 — 틀린 자료로 학습하면 그 뒤 전부가
거짓이 된다.
"""
