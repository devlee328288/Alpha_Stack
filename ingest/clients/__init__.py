"""외부 연동 — 이 프로젝트에서 바깥과 직접 통신하는 **유일한** 계층.

전부 표준 라이브러리 `urllib` 로 짜여 있다. requests·httpx 를 쓰지 않는다.
의존을 늘리지 않으려는 원본의 선택이고, 팀 프로젝트도 그대로 따른다.

| 모듈 | 출처 | 1차 프로젝트에서의 쓰임 |
|---|---|---|
| `krx_data`    | KRX OpenAPI      | ★ 시세·지수. 학습 데이터의 원천 |
| `dart_data`   | DART 공시        | ⑦ 공시 제목 감성 피처 (선택 범위) |
| `dart_report` | DART 보고서 본문 | 1차 범위 밖 |
| `ecos_data`   | 한국은행 ECOS    | 거시 피처 후보 |
| `fred_data`   | 미 연준 FRED     | 거시 피처 후보 |
| `fss_data`    | 금융감독원       | 1차 범위 밖 |
| `kosis_data`  | 국가통계포털     | 1차 범위 밖 |
| `hf_data`     | HuggingFace      | ⑦ 무료 인코더 (생성형 아님 — 절대 제약을 지킨다) |
| `yf_data`     | yfinance         | 해외 지수 대조군 |

👤 담당
------
크롤링 검수·개선 담당 팀원의 주 작업 영역이다. 1차에서 반드시 필요한 것은
`krx_data` 하나이고, 나머지는 선택 범위(⑦)와 2·3차를 위해 함께 가져왔다.
**전부 손볼 필요는 없다.** 무엇을 실제로 쓸지 킥오프에서 정한다.
"""
