"""모델 계층 — 피처 표를 받아 등락 방향을 예측한다.

    입력   X (행=시점, 열=지표) · y (+1 상승 / -1 하락)
    출력   예측 방향 배열, 그리고 가능하면 확률

② 모델 비교의 조건 — **동일 조건**
--------------------------------
필수범위 ②는 "ML 모델 성능 비교·평가"다. 비교가 성립하려면 모델만 다르고 나머지는
전부 같아야 한다. 다음이 어긋나면 비교표는 아무 말도 하지 않는다.

  · 같은 폴드 분할 (`evaluation.walk_forward` 가 만든 것을 공유한다)
  · 같은 피처 집합
  · 같은 레이블 정의
  · 같은 거래비용 가정

그래서 각 모델은 **분할을 스스로 만들지 않는다.** 밖에서 받은 인덱스를 쓴다.

1차에 비교할 것
-------------
| 모델 | 실행처 | 근거 |
|---|---|---|
| RandomForest (sklearn) | 로컬 | CPU 경로. 22코어가 그대로 쓰인다 |
| XGBoost                | 로컬 | GPU 불필요한 규모 |
| LightGBM               | 로컬 | ★ 배포판이 GPU 빌드가 아니다. 코어 수만 좌우한다 |
| LSTM/GRU (선택 ⑥)      | Colab Pro | 딥러닝 학습은 GPU 이득이 실재한다 |

⚠️ "Colab 이니까 빠르겠지"로 판단하지 않는다. 로컬이 22코어라 CPU 바운드 작업은
   Colab 런타임보다 **빠르다.** 위 표가 판단 기준이다.

⚠️ 하이퍼파라미터 탐색도 폴드 안에서
---------------------------------
전체 구간으로 그리드서치한 뒤 같은 구간에서 성능을 보고하면 그 숫자는 학습 성능이지
검증 성능이 아니다. 탐색은 학습 구간 안에서 끝내고, 검증 구간은 마지막에 한 번만 본다.
"""
