"""최소 API — 파이프라인 결과를 HTTP 로 내보낸다.

원본 개인 프로젝트는 라우터 17파일·엔드포인트 64개를 가지고 있지만 **그걸 옮기지 않았다.**
1차 프로젝트에 필요한 것만 새로 정의한다. 쓰지 않는 엔드포인트는 유지 비용만 만든다.

계약 (초안 — 킥오프에서 확정)
---------------------------
| 메서드 | 경로 | 돌려주는 것 |
|---|---|---|
| GET | `/health`               | 서버·DB 상태 |
| GET | `/ohlcv`                | 시세 조회 (종목·기간) |
| GET | `/predictions`          | 최근 예측 방향과 확률 |
| GET | `/backtest/summary`     | 폴드별 성능 + 기준선 대비 |
| GET | `/models/comparison`    | ② 모델 비교표 |

FastAPI 라서 `/docs` 에 Swagger 가 자동으로 뜬다. 별도 문서를 손으로 쓰지 않는다 —
손으로 쓴 API 문서는 반드시 코드와 어긋난다.

⚠️ 아직 구현하지 않았다
--------------------
이 파일은 **계약만** 적어 둔 상태다. 구현은 킥오프에서 아래가 정해진 뒤 시작한다.

  · 팀 Supabase 를 팔 것인가, 어떤 스키마로 할 것인가
  · 대시보드가 API 를 부를 것인가, 결과 파일을 직접 읽을 것인가
  · 배포를 언제 시작할 것인가 (현재 방침: 9/1 이후. 그전까지 전원 로컬 실행)

→ docs/회의안건.md
"""
