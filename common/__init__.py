"""공통 계층 — 설정 · 경로 · 자격증명 · 거래일 계산.

**이 패키지는 프로젝트의 어떤 모듈도 import 하지 않는다.** 그래서 순환 import 가
구조적으로 생길 수 없다. 새 모듈을 여기 넣을 때 이 규칙을 먼저 확인한다.

구성
----
| 모듈 | 하는 일 |
|---|---|
| `paths`            | 루트·data·artifacts 위치. 실행 위치(cwd)와 무관하게 같은 곳을 가리킨다 |
| `settings`         | 환경변수 기반 설정. 어느 저장소에서 읽을지(STORE_BACKEND) 등 |
| `secrets`          | API 키를 .env 에서 읽는다. **값을 로그에 남기지 않는다** |
| `db`               | Postgres 연결·쿼리 어댑터 |
| `trading_calendar` | 거래일 계산·KST 오늘·ISO 변환 |

⚠️ 원본(data-service)에서 옮겨 온 코드다
--------------------------------------
개인 프로젝트 `data-service` 의 `common/` 를 가져와 import 경로만 바꾼 상태다.
원본은 리서치 리포트 생성까지 하는 더 큰 서비스라, 여기 있는 설정 중 1차 프로젝트가
쓰지 않는 항목이 섞여 있다. **킥오프에서 팀 Supabase 를 정한 뒤** 실제로 쓰는 것만
남기고 정리한다 (→ docs/decisions/0001-repo-bootstrap.md 의 남은 과제).
"""
