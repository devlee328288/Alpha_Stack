"""성과 검증 엔진 — ★ 이 저장소에서 가장 오래 살아남을 코드.

    "맞히는 것보다 어려운 건, 맞혔다고 말해도 되는지 아는 것"

왜 이 패키지가 따로 있는가
------------------------
팀 이름이 '적층(積層)'인 이유가 여기 있다. 세 차수의 주제가 이미 정해져 있다.

    1차  주가지수 데이터 활용 머신러닝·딥러닝
    2차  나만의 로보 어드바이저 개발 **및 성과 검증**
    3차  나만의 투자 인디케이터 개발 **및 성과 검증**

셋 다 "개발 및 성과 검증"으로 끝난다. 차수마다 새로 만드는 것은 앞쪽 — 신호를 내는
방법(1차 분류기 → 2차 자산배분 → 3차 지표) — 이고, **재는 방법은 바뀌지 않는다.**
MDD 를 재는 법은 무엇이 그 수익률을 만들었든 같다.

그래서 이 패키지는 `models` 를 import 하지 않는다
---------------------------------------------
받는 것은 **숫자 배열 둘**뿐이다.

    positions   각 시점의 포지션 (+1 매수 / 0 현금 / -1 매도)
    returns     각 시점의 자산 수익률

무엇이 그 포지션을 만들었는지 모른다. LightGBM 이든, 2차의 자산배분 규칙이든,
3차의 커스텀 지표든 상관없이 같은 함수가 돈다. 이 무지(無知)가 재사용의 조건이다.

⚠️ `from models import ...` 를 쓰고 싶어지면 설계가 틀어진 신호다.
   import 를 추가하지 말고 **함수 인자를 늘린다.**

구성
----
| 모듈 | 하는 일 |
|---|---|
| `walk_forward` | 시간 순서를 지키는 분할기. 모델 불문. `gap` 기본값 = 레이블 앞보기 |
| `metrics`      | MDD · Sharpe · 승률 · 거래비용 차감 수익 |
| `baseline`     | 기준선 3종. 이걸 못 이기면 결과에 그대로 적는다 |

이 패키지가 지키는 세 가지
------------------------
1. **미래를 보지 않는다.** 분할은 언제나 시간 순이다. K-fold 를 쓰지 않는다.
   그리고 **기본값이 안전한 쪽이다** — `gap` 을 안 주면 레이블 앞보기만큼 자동으로
   벌어지고, 더 짧게 주면 `LeakageError` 다. 누수는 예외를 내지 않고 성능만 올리므로
   "빠뜨리면 위험한" 설계를 "명시해야 위험한" 설계로 뒤집었다 (요구사항 F-09).
2. **기준선과 함께 보고한다.** 정확도 단독 수치는 아무 말도 하지 않는다.
   상승이 55% 나오는 구간에서 늘 "오른다"고 답해도 55%가 나온다.
3. **비용을 뺀 뒤 말한다.** 거래비용 전 수익률은 실현할 수 없는 숫자다.
   일간 매매는 왕복 비용이 수익을 통째로 먹는 일이 흔하다.
"""

from evaluation import baseline, metrics, walk_forward
from evaluation.walk_forward import LeakageError

__all__ = ["LeakageError", "baseline", "metrics", "walk_forward"]
